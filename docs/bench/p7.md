# P7 — Qwen3.8-27B decode: golbang vs llama-server

> 측정일: **2026-08-15**. 같은 MI50, 같은 GGUF, 같은 SHA `3ac5658c7`,
> 같은 spec/비전 플래그, SCLK bitmask L5 exclusive (`0x20`, 1386 MHz) + 180W.
> 시작 유닛: `qwen3.8-27b-q6.service` (llama-server).
> 원본 JSON: `docs/bench/raw/p7-llama-pre.json`, `docs/bench/raw/p7-golbang-pre.json`
> 재현: `scripts/p7_bench.py`

temperature=0, `stream:false`, 같은 세 프롬프트.
클럭은 유휴 표시가 925 MHz여도 bitmask는 L5이며, 추론 중에 1386 MHz로 붙는다.

## 고치기 전 (코드 변경 없음)

| 밴드 | llama decode | llama draft acc | golbang decode | golbang `/metrics` Δ draft | junction (후) |
|------|-------------:|----------------:|---------------:|---------------------------:|--------------:|
| smoke16 `Reply with exactly: OK` | **23.97 t/s** | 8/8 (100%, mean 3.00) | **9.59 t/s** | 0 / 0 | 40 / 44 °C |
| para84 한 단락 | **22.88 t/s** | 39/39 (100%, mean 2.86) | **11.16 t/s** | 0 / 0 | 44 / 48 °C |
| mid200 중간 생성 | **20.52 t/s** | 88/153 (57.5%, mean 2.87) | **10.88 t/s** | 0 / 0 | 49 / 54 °C |

temperature=0 smoke 생각 머리글은 양쪽 같음:
`We need to respond to user: "Reply with exactly: OK". Need final`

### 분류

기동 로그: `scheduler budget n_batch=2048 n_ubatch=2048 prefill_max=2048 decode_max=1`.

`IterationBudget::for_context(..., n_parallel=1)` 가 `decode_max = n_parallel` 로 두고,
`BatchBuilder` 가 **드래프트 토큰까지** 그 예산에 센다. pending 1개를 넣은 뒤
초안이 잘려 `verify_and_emit` 이 한 번도 안 돈다 → `/metrics` draft 0.

~10 t/s 는 “스펙큘 없는 천장”과 같다. 커널이 아니라 플래너 버그.
