# P2 벤치 기록 — 동시성 코어

> 이 파일은 `docs/bench/p2.md` (P2 지시서 §5.6 / 완료 기준 참조).
> 구현 착수 시 아래 헤더에 측정 환경을 채우고, 결과는 **사후에 목표치를 맞추지 말고 있는 그대로** 기록한다.

## 측정 환경 (구현 시 채움)

| 항목 | 값 |
|------|-----|
| GGUF 파일 | (경로 + SHA256) |
| `n_parallel` / 슬롯 수 | |
| 프롬프트 세트 | |
| GPU 클럭 고정 여부 | |

## (a) 회귀 — golbang P1 vs P2

| 지표 | P1 순차 대기 | P2 스케줄러 |
|------|-------------|-------------|
| 후발 요청 TTFT | | |
| ITL | | |
| 총 latency | | |
| tok/s | | |

## (b) 경쟁 — llama-server 동일 조건 기록

> 처리량 우위는 필수 조건이 아니다. 동률 + 제어면 개선이면 통과.

| 지표 | golbang P2 | llama-server |
|------|-----------|--------------|
| TTFT | | |
| ITL | | |
| tok/s | | |
| VRAM | | |

## 제어면 관찰 (정성)

- 정책 교체(join/evict/우선순위)가 Rust 코드로 가능한지
- decode 중 즉시 503이 나오는지
- 취소 지연(슬롯 회수)이 decode 경계(1회)인지
